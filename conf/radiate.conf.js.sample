// Define the panels to be shown here - just add one line per screen/panel you want to add
var screenSources = [
	'/demo/a/index.html',
	'/demo/b/index.html',
	'/demo/c/index.html'
];

// the duration per panel that should be shown
var displayDuration = 1500;